last-in-first-out

```java
Stack<Integer> stack = new Stack<>();
stack.push(1);
Integer y = (Integer) stack.pop(); // return and remove first
Integer y = (Integer) stack.peek(); // return first
Integer y = (Integer) stack.search(element); // y = -1 not found else found
stack.empty();

// Rest all List method also works here
```

